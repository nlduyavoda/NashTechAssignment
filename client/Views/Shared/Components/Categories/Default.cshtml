@model IEnumerable<CategoriesVM>

@foreach (var category in Model)
{
    <div class="col-lg-3">
    <div class="categories__item set-bg" data-setbg="@category.pathImage">
        <h5><a href="#">@category.Name</a></h5>
    </div>
</div>
}
